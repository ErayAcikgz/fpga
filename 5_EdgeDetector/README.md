# 5_EdgeDetector
Detect a new debounced button press and generate a single event from it.

This project builds on `4_ButtonDebounce`. Instead of asking whether the button is currently pressed, it detects when the button transitions from released to pressed.
