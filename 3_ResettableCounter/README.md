# 3_ResettableCounter
Create a resettable 6-bit binary counter on the Tang Nano 9K.

The design increments a visible LED counter every 0.25 seconds. Pressing the reset button clears the counter back to zero.
